{
    "version": 3,
    "configurePresets": [
        {
            "name": "optLib",
            "hidden": false,
            "description": "Build the library with cli manager",
            "generator": "Unix Makefiles",
            "binaryDir": "${sourceDir}/build",
            "cacheVariables": {
                "OPTLIB": "ON"
            }
        }
    ]
}